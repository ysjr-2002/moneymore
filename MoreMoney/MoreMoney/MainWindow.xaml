<Window
    x:Class="MoreMoney.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MoreMoney"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Name="window"
    Title="Run_Test"
    Width="800"
    Height="500"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="300" />
            <Setter Property="Height" Value="24" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style BasedOn="{StaticResource MyButton}" TargetType="Button">
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="5" />
        </Style>

        <Style TargetType="Label">
            <Setter Property="Width" Value="120" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Right" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="24" />
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="5"
            Orientation="Horizontal">
            <Label Content="Cash-&gt;Select port:" />
            <ComboBox
                Name="cmbPorts"
                Width="100"
                Height="24" />
            <Button
                Name="btnOpenPort"
                Click="btnOpenPort_Click"
                Content="Open" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Margin="5"
            Orientation="Horizontal">
            <Label Content="Card-&gt;Select port:" />
            <ComboBox
                Name="cmbICPorts"
                Width="100"
                Height="24" />
            <Button
                Name="btnICOpenPort"
                Click="btnICOpenPort_Click"
                Content="Open" />
        </StackPanel>

        <WrapPanel
            Grid.Row="2"
            Width="{Binding ActualWidth, ElementName=window}"
            Orientation="Horizontal">
            <Button Click="btnReset_Click" Content="Reset" />
            <Button Click="btnReadId_Click" Content="Read CassetteId" />
            <Button Click="btnOpenCassette_Click" Content="OpenCassette" />
            <Button Click="btnCloseCassette_Click" Content="CloseCassette" />
            <Button Click="btnReadProgram_Click" Content="ReadProgram" />
            <Button Click="btnReadCounter_Click" Content="ReadCounter" />
            <Button Click="btnSelfTest_Click" Content="SelfTest" />
        </WrapPanel>

        <WrapPanel
            Grid.Row="3"
            Width="{Binding ActualWidth, ElementName=window}"
            Orientation="Horizontal">

            <Label Content="Hopper number:" />
            <ComboBox Name="cmbHn" SelectedIndex="1">
                <ComboBoxItem Content="1" />
                <ComboBoxItem Content="2" />
                <ComboBoxItem Content="3" />
                <ComboBoxItem Content="4" />
            </ComboBox>

            <Label Content="number of notes to pickup:" />
            <TextBox
                Name="txtMoney"
                Width="30"
                MaxLength="3"
                Text="30" />
            <Button Click="btnMoveForeward_Click" Content="Move Forward" />

        </WrapPanel>

        <RichTextBox
            Name="log"
            Grid.Row="4"
            Margin="5,0"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollBarVisibility="Auto">
            <RichTextBox.Resources>
                <Style TargetType="RichTextBox">
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Background" Value="Black" />
                    <Setter Property="Foreground" Value="#00ff00" />
                </Style>
                <Style TargetType="Paragraph">
                    <Setter Property="Margin" Value="0" />
                </Style>
            </RichTextBox.Resources>
            <FlowDocument />
        </RichTextBox>
    </Grid>
</Window>
